services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: publicmind
      POSTGRES_USER: publicmind
      POSTGRES_PASSWORD: publicmind
    ports:
      - "5432:5432"
    volumes:
      - publicmind-postgres:/var/lib/postgresql/data

volumes:
  publicmind-postgres:

